name: lingua_voice
description: AI language tutor with voice conversation
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  record: ^6.0.0
  audioplayers: ^6.1.0
  dio: ^5.4.0
  provider: ^6.1.0
  path_provider: ^2.1.0
  permission_handler: ^12.0.0
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
